# 环境配置

`testing-run` 执行前的环境前置。只允许本地 / test / local profile，禁止指向生产或远程库。

## 后端

- 启动命令：{{BACKEND_RUN}}
- profile：test / local（禁止 prod）
- 端口：待补充
- 健康检查：`curl -sS http://localhost:<port>/actuator/health`

## 数据库

- 类型：{{DATABASES}}
- 连接：仅本地 / test schema / Testcontainers（禁止 DROP/TRUNCATE/全表 DELETE）
- 迁移工具：{{DB_TOOLS}}

## 前端

- 启动命令：{{FRONTEND_RUN}}
- base URL / 接口代理：待补充

## 校验清单

- [ ] 数据库连接确认不是生产库
- [ ] profile 为 test/local
- [ ] 端口未被占用（`lsof -iTCP -sTCP:LISTEN -n -P`）
